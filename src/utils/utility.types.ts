export enum EDateFormats {
  'mm/dd/YYYY' = '%m/%d/%Y',
  'dd/mm/YYYY' = '%d/%m/%Y',
  'YYYY/mm/dd' = '%Y/%m/%d',
  'YYYY/dd/mm' = '%Y/%d/%m',
  'mm-dd-YYYY' = '%m-%d-%Y',
  'dd-mm-YYYY' = '%d-%m-%Y',
  'YYYY-mm-dd' = '%Y-%m-%d',
  'YYYY-dd-mm' = '%Y-%d-%m',
}
