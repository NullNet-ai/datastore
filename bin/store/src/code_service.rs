// This file is @generated by prost-build.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct GetCodeRequest {
    /// database or project name (e.g. "default")
    #[prost(string, tag = "1")]
    pub database: ::prost::alloc::string::String,
    /// entity/table name (e.g. "organizations", "sessions")
    #[prost(string, tag = "2")]
    pub table: ::prost::alloc::string::String,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct GetCodeResponse {
    #[prost(string, tag = "1")]
    pub code: ::prost::alloc::string::String,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CounterConfig {
    /// table/entity name
    #[prost(string, tag = "1")]
    pub entity: ::prost::alloc::string::String,
    #[prost(string, tag = "2")]
    pub prefix: ::prost::alloc::string::String,
    #[prost(int32, tag = "3")]
    pub default_code: i32,
    /// zero-pad counter to this many digits; 0 = no padding, use default_code + counter
    #[prost(int32, tag = "4")]
    pub digits_number: i32,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct InitCountersRequest {
    /// database or project name
    #[prost(string, tag = "1")]
    pub database: ::prost::alloc::string::String,
    #[prost(message, repeated, tag = "2")]
    pub counters: ::prost::alloc::vec::Vec<CounterConfig>,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct InitCountersResponse {
    #[prost(bool, tag = "1")]
    pub success: bool,
    /// error details if success is false
    #[prost(string, tag = "2")]
    pub message: ::prost::alloc::string::String,
}
/// Generated client implementations.
pub mod code_service_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value
    )]
    use tonic::codegen::http::Uri;
    use tonic::codegen::*;
    #[derive(Debug, Clone)]
    pub struct CodeServiceClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl CodeServiceClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> CodeServiceClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::BoxBody>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> CodeServiceClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::BoxBody>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::BoxBody>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<http::Request<tonic::body::BoxBody>>>::Error:
                Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            CodeServiceClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// / Returns the next unique code for the given database and entity (table).
        /// / Uses Redis INCR atomically. Config (prefix, default_code, digits_number) must be set via InitCounters first.
        pub async fn get_code(
            &mut self,
            request: impl tonic::IntoRequest<super::GetCodeRequest>,
        ) -> std::result::Result<tonic::Response<super::GetCodeResponse>, tonic::Status> {
            self.inner.ready().await.map_err(|e| {
                tonic::Status::unknown(format!("Service was not ready: {}", e.into()))
            })?;
            let codec = tonic::codec::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static("/code_service.CodeService/GetCode");
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("code_service.CodeService", "GetCode"));
            self.inner.unary(req, path, codec).await
        }
        /// / Initializes or updates counter config for entities. Optionally sets initial counter value.
        pub async fn init_counters(
            &mut self,
            request: impl tonic::IntoRequest<super::InitCountersRequest>,
        ) -> std::result::Result<tonic::Response<super::InitCountersResponse>, tonic::Status>
        {
            self.inner.ready().await.map_err(|e| {
                tonic::Status::unknown(format!("Service was not ready: {}", e.into()))
            })?;
            let codec = tonic::codec::ProstCodec::default();
            let path =
                http::uri::PathAndQuery::from_static("/code_service.CodeService/InitCounters");
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("code_service.CodeService", "InitCounters"));
            self.inner.unary(req, path, codec).await
        }
    }
}
