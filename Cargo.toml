[workspace]
members = [
    "libs/hlc",
    "libs/merkle",
    "bin/main",
    "bin/store",
    "bin/server",
    "bin/store-generator"
]
resolver = "2"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true